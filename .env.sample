OPENAI_API_KEY=your_api_key
# Stripe Billing (optional)
STRIPE_SECRET_KEY=
STRIPE_PRICE_ID_MONTHLY=
STRIPE_PRICE_ID_ANNUAL=
STRIPE_WEBHOOK_SECRET=
APP_URL=http://localhost:3000
FREE_SNAPSHOT_LIMIT=3
NEXT_PUBLIC_METERED_PAYWALL=0
# Optional: Upstash Redis for rate limiting (fallback to in-memory if unset)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Rate limit defaults (tuned in code):
# Dashboard: 60/min; Snapshots: 20/min; Login flow: 10/min
